name: flutter_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  logging: ^1.2.0       # ✅ top-level dependency
  sqflite: ^2.3.0       # ✅ top-level dependency
  path: ^1.9.0          # ✅ top-level dependency
  intl: ^0.18.1         # ✅ top-level dependency
  cupertino_icons: ^1.0.8  # ✅ top-level dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
